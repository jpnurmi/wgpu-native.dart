import 'enums.dart';

typedef BufferMapCallback = void Function(BufferMapAsyncStatus status);

typedef LogCallback = void Function(int level, String message);

typedef RequestAdapterCallback = void Function(int adapterId);
